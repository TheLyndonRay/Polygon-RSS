<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.solaris.lyndon.rss.SettingsActivity">

    <TextView
        android:id="@+id/font_color_label"
        android:text="@string/font_color"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="15dp"
        android:paddingBottom="15dp"
        android:textSize="20sp"/>
    <Spinner
        android:id="@+id/font_color_spinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/font_color_label">
    </Spinner>
    <TextView
        android:id="@+id/font_size_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/font_color_label"
        android:text="@string/font_size"
        android:paddingTop="15dp"
        android:paddingBottom="15dp"
        android:textSize="20sp"/>
    <Spinner
        android:id="@+id/font_size_spinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/font_color_spinner"
        android:layout_toRightOf="@id/font_size_label">
    </Spinner>

</RelativeLayout>
